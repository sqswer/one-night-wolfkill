# 一夜狼人杀在线版 · 部署指南

> **2026 年 8 月核实结论：免费免卡且能跑持久 Node.js 进程的平台仅剩以下两个小平台。**
> 主流平台全部阵亡：Koyeb（被收购无免费档）、Glitch（关停）、Render（要卡）、Hugging Face Spaces Docker/Gradio（要 PRO $9/月）、Northflank/Railway/Fly.io/Oracle（全要卡）。

---

## 方案 A：Bonto（推荐，最简单）

- **费用**：免费，无需信用卡
- **配置**：50 小时/月，128MB RAM，0.25 CPU，自动休眠 30 分钟，唤醒约 2–5 秒
- **支持**：Node.js 原生运行，WebSocket/SSE 均可，浏览器内置编辑器
- **地址**：`https://<app名>.bonto.run`

### 步骤
1. 打开 https://bonto.dev 注册（免卡）。
2. 点 **Create New App** → 选 **Import from GitHub**（或直接在浏览器编辑器粘贴代码）。
3. 上传 `server.js`、`package.json`、`public/` 全部文件。
4. 设置 Start Command 为 `node server.js`。
5. Deploy 完成，获得 `xxx.bonto.run` 公网地址，手机打开即可游玩。

> 一局游戏约 15–30 分钟，在休眠窗口内；局间重建房间即可（内存态，重启清空属正常）。

---

## 方案 B：SnapDeploy（备选，Docker 方式）

- **费用**：免费，无需信用卡
- **配置**：100 小时/月（或 10 次部署/天），512MB RAM，自动休眠 15–45 分钟，唤醒约 10–60 秒
- **支持**：Docker 容器，连 GitHub 自动构建

### 步骤
1. 打开 https://snapdeploy.dev 注册（免卡）。
2. 点 **Create New Container** → **Import from GitHub** → 选仓库。
3. SnapDeploy 自动检测 `Dockerfile` 并构建。
4. 构建成功后获得公网地址。

---

## 备选：自有服务器 / VPS

```bash
git clone <你的仓库> && cd 一夜狼人杀在线网页版
node server.js
# 或 Docker：docker build -t oww . && docker run -d -p 3000:3000 oww
```
