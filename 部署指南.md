# 一夜狼人杀在线版 · 部署指南

> **唯一推荐：Hugging Face Spaces（Docker）**——免费、免信用卡、2 vCPU / 16GB RAM、48 小时才休眠。

## 前置条件

- 本项目代码已推到 GitHub 仓库（含 `Dockerfile`、`server.js`、`package.json`、`public/` 全部文件）
- 注册 https://huggingface.co 账号（免卡，支持 GitHub/Google 登录）

## 部署步骤

1. 登录 Hugging Face → 右上角头像 → **New Space**
2. 填写：
   - **Name**：`one-night-werewolf`（或自定义）
   - **SDK**：选 **Docker**
   - **Template**：选 **Blank**
   - **Hardware**：保持 **Free**（CPU Basic: 2 vCPU, 16GB RAM）
3. 点 **Create Space**
4. 进入 Space 页面 → **Files** 标签，把仓库全部文件上传（或本地 `git clone` 后 push 到 HF 的 Git 地址）
5. 等待构建完成（**Logs** 标签查看进度），成功后获得公网地址：

```
https://<你的用户名>-one-night-werewolf.hf.space
```

6. 手机浏览器打开该地址即可创建/加入房间、联机游玩。

## 说明

- HF Spaces 自动注入 `PORT=7860`，`server.js` 已读取 `process.env.PORT`，无需改代码。
- 免费档 URL 固定为 `*.hf.space`，不能绑自定义域名。
- 空闲 48 小时后容器休眠，下次访问冷启动约 30–90 秒；有人玩就不会休眠。
- 房间数据存内存，容器重启后旧房间清空，每局重新建房即可。

## 备选：自有服务器 / VPS

```bash
git clone <你的仓库> && cd 一夜狼人杀在线网页版
node server.js
# 或 Docker：docker build -t oww . && docker run -d -p 3000:3000 oww
```
